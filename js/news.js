document.addEventListener('DOMContentLoaded',()=>{const n=window.CCAM_APP_DATA.news||[];document.getElementById('news').innerHTML=n.map(x=>`<article class="card newsitem"><span class="pill p-med">${x.tag||x.source}</span><h3><a href="${x.url}" target="_blank" rel="noopener">${x.title}</a></h3><p class="small">${x.source} · ${new Date(x.date).toLocaleDateString('fr-FR')}</p></article>`).join('')||'<div class="note">Aucune actualité embarquée. Lancez le workflow de veille pour reconstruire cette page.</div>'});
